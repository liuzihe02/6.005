# Lambda Expressions

We use the `->` to define a short function.

```java
(a, b) -> a + b;
```

or if only one parameter

```java
x -> x * x;
```

or no parameters
```java
() -> System.out.println("Hello, World!");
```

